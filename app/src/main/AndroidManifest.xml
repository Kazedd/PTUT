<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matches">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data android:name="com.google.android.geo.API_KEY" android:value="AIzaSyCX0Jmta-vEleEiNafBqSFax8IYg6kL0tM"/>
        <activity android:name=".profil"></activity>
        <activity android:name=".RegisterEntrepriseActivity" />
        <activity android:name=".EntrepriseOuEtudiant" />
        <activity android:name=".RegistersActivity" />
        <activity android:name=".matchActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>